language: c
compiler:
  - gcc
  - clang

before_install:
  # Get CMake >=2.8.8
  - sudo add-apt-repository -y ppa:kalakris/cmake
  - sudo apt-get update -qq
  - sudo apt-get install -qq cmake libhdf5-serial-dev

before_script:
  - cmake .

script:
  - make
  - make test
